# 2026-05-31 SolidWorks C# 强类型自动化突破

## 完成内容

### 阶段1：C# 强类型连接与 FeatureExtrusion2（23参数）
- 编译器：`C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`（C# 5）
- 连接方式：`Marshal.GetActiveObject("SldWorks.Application")`
- 成功创建 100x100x50mm 方块拉伸特征

### 阶段2：FeatureCut4（27参数）+ SelectByRay
- 用纯反射探测工具 `ProbeSignaturePure.cs` 确认 `SelectByRay` 正确签名（11参数）
- `TypeWanted = 1`（swSelFACES）才能选到面
- 成功在方块顶面贯穿切除直径30mm圆孔
- 截图确认模型正确生成

### 关键踩坑记录

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| FeatureExtrusion2编译报错 | 参数数量错误（22 vs 23） | 逐行对齐23个参数 |
| SelectByRay选面失败 | TypeWanted=0无效 | 改为1（swSelFACES） |
| 日志成功但SW空白 | 权限隔离（管理员vs普通） | 统一普通权限运行 |
| 文档自动关闭 | finally块有CloseAllDocuments | 删除关闭逻辑 |
| C# 5编译报错 | 误用`$""`插值 | 全部改用string.Format() |

### 生成文件
- `Stage2_Test.cs` / `Stage2_Test.exe` — 阶段1+2完整流程
- `ProbeSignaturePure.cs` — 纯反射API签名探测工具
- `log.txt` — 运行日志（Console.SetOut重定向）

### Skill更新
- `solidworks-automation` v3.3.0 → v3.4.0（新增C#强类型调用规范章节）
- `sw-designer` v2.2.0 → v2.3.0（新增架构选择决策树和验证机制）

---

## 阶段3：叉形接头（Clevis Joint）全自动建模攻坚（下午-晚上）

### 突破性成果
1. **面遍历算法**：`GetBodies2` → `GetFaces` → `GetBox` 几何极值定位 → `Entity.Select4` 选中
   - 替代不可靠的 `SelectByRay` 射线法
   - 成功定位后端面（minX）和顶面（maxY）
2. **步骤1+2 验证通过**：叉部 90×50×50mm + 柄部 70×50×25mm 居中拉伸 ✅
3. **进程连接升级**：`Activator.CreateInstance` 替代 `Marshal.GetActiveObject`（彻底解决权限隔离）

### 残余 Bug
- [P0] 步骤3 `CreateArc` 半圆切除切成"拱门" → 明天修复：圆心 X=-0.045m
- [P0] 步骤4 U 形槽坐标微调 → 确认 Z 轴居中参数
- [P1] Φ18 通孔（柄部+叉部双侧）尚未实现

### 编译环境确认
- CSC: `C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`
- DLL: `E:\sw2024\SOLIDWORKS\api\redist\`
- 反射探测工具 `Probe_Signature.exe` 可编译运行 ✅

### Skill更新（睡前知识沉淀）
- `solidworks-automation` v4.0.0 → v4.1.0（新增第二十章：面遍历+权限隔离+双语兼容+C#5铁律+残余Bug记录）
- `sw-designer` v2.2.0 → v2.3.0（新增第十三章：SW自动化设计避坑指南）
- MEMORY.md 同步更新面遍历算法、Activator.CreateInstance、双语选面、编译命令

### 生成文件
- `Clevis_Joint.cs` / `Clevis_Joint.exe` — 叉形接头建模（步骤1+2已通过，步骤3+4待修复）
- `Probe_Sig.cs` / `Probe_Sig.exe` — API 签名反射探测工具
- `Clevis_Joint_Arch.cs` — 架构重构版（NewDocument + FeatureByName + 熔断）
- `clevis_log.txt` / `testbox_log.txt` / `probe_log.txt` — 运行日志
